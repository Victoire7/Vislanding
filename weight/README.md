# 预训练 Backbone 权重

训练与 demo 需要 ViT-small 预训练权重（文件名示例：`metric_depth_vit_small_800k.pth`）。

请从项目 Release 页面或团队提供的网盘下载，放置为：

```
weight/metric_depth_vit_small_800k.pth
```

Fine-tune 后的 Safe 学生权重见 `final_model/README.md`。
